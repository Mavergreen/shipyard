name: release

on:
  push:
    branches: [main]

permissions:
  contents: write

concurrency:
  group: >-
    release-${{ github.event_name == 'pull_request'
                && github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  build:
    runs-on: macos-26   # the CMake modules gate on Apple clang; Linux cannot exercise them
    timeout-minutes: 30
    outputs:
      version: ${{ steps.ver.outputs.version }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0        # shipyard-version.sh counts commits

      - name: Install mavericks-shipyard (self)
        uses: ./.github/actions/install

      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
      - name: Provision PyYAML (the workflow-parse gate's only dependency)
        run: python3 -m pip install --quiet pyyaml

      - name: Resolve the version
        id: ver
        run: |
          set -eu
          out="$(sh scripts/shipyard-version.sh)"
          ver="$(printf '%s\n' "$out" | sed -n 's/^FULL=//p')"
          printf '%s\n' "$ver" > VERSION      # a build product; gitignored, read by CMakeLists
          echo "version=$ver" >> "$GITHUB_OUTPUT"
          echo "shipyard $ver"

      - name: ctest (parameterized suites)
        run: |
          cmake -S . -B "$RUNNER_TEMP/build"
          ctest --test-dir "$RUNNER_TEMP/build" --output-on-failure

      - name: Standalone test suites
        run: sh scripts/run-repo-tests.sh

      - name: Our own conventions gate
        run: sh scripts/check-family-conventions.sh

      - name: Build both updater slices (native x86_64/10.9, cross arm64)
        run: |
          set -eu
          # platform: CMAKE_OSX_ARCHITECTURES and the deployment target are directory-scoped, so this
          #           needs two configures, not two targets -- and arm64 has no 10.9. /usr/bin/clang
          #           pins the Apple toolchain regardless of what else the runner image puts on PATH.
          cmake -S . -B "$RUNNER_TEMP/upd-native" -DSHIPYARD_BUILD_UPDATER=ON \
            -DMAVERICKS_ALLOW_GENERIC_ICON=ON -DCMAKE_OBJC_COMPILER=/usr/bin/clang \
            -DCMAKE_OSX_ARCHITECTURES=x86_64 -DCMAKE_OSX_DEPLOYMENT_TARGET=10.9
          cmake --build "$RUNNER_TEMP/upd-native"
          cmake -S . -B "$RUNNER_TEMP/upd-cross" -DSHIPYARD_BUILD_UPDATER=ON \
            -DMAVERICKS_ALLOW_GENERIC_ICON=ON -DCMAKE_OBJC_COMPILER=/usr/bin/clang \
            -DCMAKE_OSX_ARCHITECTURES=arm64 -DCMAKE_OSX_DEPLOYMENT_TARGET=11.0
          cmake --build "$RUNNER_TEMP/upd-cross"

      - name: Package
        run: |
          set -eu
          v='${{ steps.ver.outputs.version }}'
          mkdir -p dist
          # platform: MAVERICKS_NO_USER_REGISTRY keeps this configure from re-pointing the runner's
          #           CMake user package registry (the self-install step above set it) at a staging
          #           prefix; the pkg's postinstall registers the INSTALLED payload instead.
          cmake -S . -B "$RUNNER_TEMP/payload-build" -DMAVERICKS_NO_USER_REGISTRY=ON
          cmake --install "$RUNNER_TEMP/payload-build" --prefix "$RUNNER_TEMP/payload-prefix"
          sh scripts/package-pkg.sh \
            --payload "$RUNNER_TEMP/payload-prefix/share/cmake/MavericksShipyard" \
            --app-native "$RUNNER_TEMP/upd-native/MavericksShipyardUpdater.app" \
            --app-cross  "$RUNNER_TEMP/upd-cross/MavericksShipyardCrossUpdater.app" \
            --version "$v" \
            --out "dist/mavericks-shipyard-$v.pkg"
          sh scripts/assert_pkg_installs_in_place.sh "dist/mavericks-shipyard-$v.pkg"

      - name: Release notes
        run: |
          set -eu
          v='${{ steps.ver.outputs.version }}'
          sh "$SHIPYARD_SCRIPTS/release-notes.sh" --tag "v$v" --version "$v" \
            --product Shipyard --min-os 10.9.5 --out dist/RELEASE_NOTES.md
          [ -s dist/RELEASE_NOTES.md ] || { echo "::error::release notes came back empty"; exit 1; }

      - name: Sign + appcast
        env:
          SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}
          GH_TOKEN: ${{ github.token }}   # sign_and_appcast.sh fetches ed25519-sign via gh
        run: |
          set -eu
          v='${{ steps.ver.outputs.version }}'
          sh scripts/sign_and_appcast.sh \
            --channel-title "Mavericks Shipyard" \
            --version "$v" \
            --pkg-url "https://github.com/ModernMavericks/shipyard/releases/download/v$v/mavericks-shipyard-$v.pkg" \
            --notes-file dist/RELEASE_NOTES.md \
            --pkg "dist/mavericks-shipyard-$v.pkg" \
            > dist/appcast.xml
          # spec: modernmavericks-conventions SKILL.md, "A self-upstream repo gets a real baseline
          #       too, via --tag-glob" -- this is the worked example the rule book names.
          sh scripts/assert_appcast_upgradeable.sh --appcast dist/appcast.xml --version "v$v" --tag-glob 'v*.*.*'

      - name: Artifact conformance
        run: |
          set -eu
          v='${{ steps.ver.outputs.version }}'
          sh scripts/artifact-facts.sh dist "$v" | sh scripts/check-artifact-conformance.sh
          ls -l dist

      # platform: if Installer ran the scripts under Rosetta, the postinstall would keep the x86_64
      #           updater and delete the arm64 one with every gate above still green -- this runner is
      #           the only Apple Silicon box that sees the pkg before a developer's, and the 10.9 box
      #           can neither build nor exercise the arm64 slice, so the slices and payload are
      #           HARD-asserted here. Registration is only WARNED on: it runs as the console user
      #           through their login shell, and this runner's login environment is not a developer's.
      - name: Install smoke (the pkg, on Apple Silicon)
        timeout-minutes: 10
        run: |
          set -eu
          v='${{ steps.ver.outputs.version }}'
          apps="/Library/Application Support/ModernMavericks"
          agents=/Library/LaunchAgents
          payload=/usr/local/mavericks-shipyard
          bad=0; show_log=0
          fail() { echo "::error::install smoke: $*"; bad=1; }
          scripts_log() {
            echo "---- /var/log/install.log, the pkg's scripts ----"
            sudo grep -E 'preinstall|postinstall|mavericks-shipyard' /var/log/install.log | tail -n 100 || true
          }
          sudo installer -pkg "dist/mavericks-shipyard-$v.pkg" -target / || fail "installer itself failed"
          [ -f "$agents/dev.modernmavericks.mavericks-shipyard-cross-updatecheck.plist" ] || fail "the arm64 slice's LaunchAgent is missing"
          [ -d "$apps/MavericksShipyardCrossUpdater.app" ] || fail "MavericksShipyardCrossUpdater.app is missing"
          [ ! -e "$agents/dev.modernmavericks.mavericks-shipyard-updatecheck.plist" ] || fail "the x86_64 slice's LaunchAgent survived; launchd would run it, under Rosetta, at every login"
          [ ! -e "$apps/MavericksShipyardUpdater.app" ] || fail "MavericksShipyardUpdater.app (x86_64) survived on Apple Silicon"
          [ -f "$payload/MavericksShipyardConfig.cmake" ] || fail "$payload/MavericksShipyardConfig.cmake is missing"
          [ -f "$payload/scripts/register-with-cmake.sh" ] || fail "$payload/scripts/register-with-cmake.sh is missing"
          # platform: lipo -info, not -archs -- 10.9's lipo has no -archs (check-shell-portability.sh bans it).
          exe="/Library/Application Support/ModernMavericks/MavericksShipyardCrossUpdater.app/Contents/MacOS/MavericksShipyardCrossUpdater"
          lipo -info "$exe" | grep -q 'is architecture: arm64$' || fail "the remaining updater is not a thin arm64 executable: $(lipo -info "$exe" 2>&1)"
          reg="$HOME/.cmake/packages/MavericksShipyard/mavericks-shipyard"
          if [ ! -f "$reg" ]; then
            echo "::warning::install smoke: no registry entry at $reg -- the postinstall's registration (as the console user, via their login shell) never ran or found no cmake"
            show_log=1
          elif [ "$(cat "$reg")" != "$payload" ]; then
            echo "::warning::install smoke: $reg points at $(cat "$reg"), not $payload -- the postinstall's registration did not reach this user or found no cmake on their login PATH"
            show_log=1
          else
            echo "registry: $reg -> $payload"
          fi
          if [ "$bad" -ne 0 ] || [ "$show_log" -ne 0 ]; then scripts_log; fi
          [ "$bad" -eq 0 ] || exit 1
          echo "install smoke: arm64 updater kept, x86_64 updater removed, payload in $payload"

      - uses: actions/upload-artifact@v7
        with:
          name: shipyard-dist
          path: dist

  scan:
    needs: build
    if: always()        # a build that fails after signing left logs too; scan them
    permissions:
      actions: read     # this run's job logs
      contents: read
    uses: ./.github/workflows/scan-for-key.yml
    with:
      artifact: shipyard-dist
      shipyard-ref: ${{ github.sha }}
    secrets:
      SPARKLE_PRIVATE_KEY: ${{ secrets.SPARKLE_PRIVATE_KEY }}

  publish:
    needs: [build, scan]
    permissions:
      contents: write
    uses: ./.github/workflows/publish-release.yml
    with:
      version: v${{ needs.build.outputs.version }}
      artifact: shipyard-dist
      shipyard-ref: ${{ github.sha }}

  major-tag:
    needs: [build, publish]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Move the major tag
        run: |
          set -eu
          major="v${{ needs.build.outputs.version }}"; major="${major%%.*}"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f -a "$major" -m "$major -> v${{ needs.build.outputs.version }}"
          git push -f origin "$major"
          echo "moved $major -> $(git rev-parse --short HEAD)"
